# Blueprint Stage

This folder is intentionally scaffold-only during stage one.

Do not generate skill blueprints here during the extraction run unless stage two is explicitly requested.

When stage two begins, use the files in `../AI/Channel/` as the primary source material.
